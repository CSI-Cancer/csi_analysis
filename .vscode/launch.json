{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Pretrain Debug",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/csi_analysis/training/channel_classifier/channel_classifier/modeling/pretrain.py",
            "console": "integratedTerminal",
            "justMyCode": false,
            "python": "${workspaceFolder}/.venv/bin/python",  // Use your project's .venv
            "env": {
                "PYTHONPATH": "${workspaceFolder}/csi_analysis/training/channel_classifier",
                "VIRTUAL_ENV": "${workspaceFolder}/.venv",
                "PATH": "${workspaceFolder}/.venv/bin:${env:PATH}"
            }
        }
    ]
}